{
  "ignorePatterns": [
    { "pattern": "^http://localhost" },
    { "pattern": "^https://acct\\." },
    { "pattern": "internal\\.example\\.com" },
    { "pattern": "^arn:aws:" }
  ],
  "timeout": "10s",
  "retryOn429": true,
  "retryCount": 3
}
